// Get the URL upon load
// const thisURL = window.location.href;

const sendURL = function() {
  // Send off the url
}

// If thisURL.includes("youtube") - first check if it has an id, then make youtube API request using the id.

